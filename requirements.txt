# Core
streamlit==1.36.0

# WebRTC with explicit no-av option
streamlit-webrtc==0.47.1 ; python_version >= '3.7'

# Video processing alternatives
opencv-python-headless==4.9.0.80
imageio==2.34.0
imageio-ffmpeg==0.4.9  # FFmpeg support without PyAV

# Audio processing
pydub==0.25.1
soundfile==0.12.1
gTTS==2.4.0

# Data processing
pandas==2.2.2
numpy==2.0.0

# Video editing (configure to not use PyAV)
moviepy==1.0.3 --no-deps
