# Core requirements
streamlit==1.36.0
streamlit-webrtc==0.47.1

# AV alternatives (try these if av fails)
opencv-python-headless==4.9.0.80
imageio==2.34.0
av==10.0.0


# Audio/Video processing
moviepy==1.0.3
gTTS==2.5.1
pydub==0.25.1

# Data processing
pandas==2.2.2
numpy==2.0.0

# Image processing
Pillow==10.4.0  # Newer version with Python 3.13 support

# System dependencies (via pip)
pkgconfig==1.5.5
